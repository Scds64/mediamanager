# Media/manager（mmbot）构建推送指南

> 本文档供其他 agent 或协作者使用：如何把代码改动（含前端模板）发布成新版本镜像。
> 最后更新：2026-09-02（版本 0.3.10）

## 1. 项目结构速览

| 项 | 位置/值 |
|---|---|
| 源码仓库（git） | `D:\Desktop\2个项目\123bot重构`（分支 `main`） |
| 远端仓库 | `https://github.com/Scds64/mediamanager.git`（**私有**） |
| 版本号 | `cmd/mmbot/main.go` 第 217 行 `var version = "x.y.z"` |
| 前端模板 | `templates/index.html`、`templates/login.html`（**打进镜像，不需挂载**） |
| 静态资源 | `static/`（镜像内置） |
| 默认配置模板 | `templete.env`（镜像内置，首启自动生成 `config/user.env`） |
| Docker Hub 仓库 | `ssccing/mediamanager`（账号 `ssccing`，密码 `songchen521.`，**末尾带点**） |
| 发布方式 | GitHub Actions 云端构建多架构镜像 → 推 Hub（**主通道**，0.3.8 起） |
| git 提交身份 | `ssccing <ssccing@users.noreply.github.com>` |

## 2. 发布前置检查（每次必做）

1. **确认 Clash 代理已开**：git 仓库级代理 `http://127.0.0.1:7897`。代理没开 push 必失败。
   检查：`curl -x http://127.0.0.1:7897 -s -o /dev/null -w "%{http_code}" https://api.github.com` 返回非 `000` 即通。
2. **确认当前版本号是最新的**：`grep "var version" cmd/mmbot/main.go`，看 Hub `latest` 现在是什么版本，新版本必须递增。

## 3. 发布流程（标准步骤）

```bash
# 进入源码目录（Git Bash 下）
cd "D:\Desktop\2个项目\123bot重构"

# ① 确认改动内容（应该只有你预期的文件）
git status --short

# ② 递增版本号（编辑 cmd/mmbot/main.go）
#    var version = "0.3.10"  →  var version = "0.3.11"
#    每次发布必须递增，启动日志 / TG 通知 / /update 判断都依赖它

# ③ 编译验证（必须在源码目录）
go build ./...
# 输出无错误即通过

# ④ 提交并推送（身份用 -c 传入，别用全局身份）
git add -A
git -c user.name=ssccing -c user.email=ssccing@users.noreply.github.com commit -m "v0.3.11: <改动简述>"
git push origin main
# push 会触发 GitHub Actions 自动构建（无需本地 Docker）
```

## 4. 等待构建并验证

```bash
# ④ 找到本次 push 触发的 Actions run 并等待完成（约 10~15 分钟）
export PATH="$PATH:/c/Program Files/GitHub CLI"
gh run list --repo Scds64/mediamanager --limit 1
gh run watch <run_id> --repo Scds64/mediamanager --exit-status --interval 30
# 看到 "✓ Complete job" 即成功；有 ANNOTATIONS 的 Node 版本弃用警告可忽略

# ⑤ 验证 Hub 标签已更新（latest 双平台）
curl -x http://127.0.0.1:7897 -s "https://hub.docker.com/v2/repositories/ssccing/mediamanager/tags/latest" \
  | python -c "import sys,json;d=json.load(sys.stdin);print([ (i['architecture'],i['digest'][:12],i['last_pushed']) for i in d['images']])"
# 应看到 amd64 + arm64 两条，last_pushed 是刚更新的时间
```

## 5. NAS 部署（用户自理）

**不要 SSH 到 NAS 部署**——NAS SSH 密码 `password` 已失效（2026-09-02），更新由用户自己处理：

- 用户 TG 发 `/update`（自动拉新镜像 + 重建容器），或
- 手动：`docker pull ssccing/mediamanager:latest && cd /mnt/mydisk/mediaman && docker-compose up -d`

验证：`docker logs Mediamanager 2>&1 | head -2` 显示新版本号。

## 6. 常见坑（务必注意）

1. **代理没开 = push 失败**。报错特征：`Failed to connect to github.com:443 over proxy 127.0.0.1`。让用户开 Clash 再重试。
2. **gitignore 必须锚定根目录**：`/data/ /log/ /db/ /strm/ /mmbot /mmbot_linux`。加 ignore 规则时检查是否与源码目录同名（`strm/` 会误伤 `internal/strm/`、`mmbot` 会误伤 `cmd/mmbot/`——0.3.8 曾两次构建失败）。
3. **`latest` 标签不能删**，它是 /update 与 compose 的拉取目标；日期标签 `YYYYMMDD` 留作回滚。
4. **secrets 是 workflow run 创建时快照的**：改过 `DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN` 后，之前排队的 run 会读到空值登录失败（`Password required`），须手动 `gh workflow run` 重跑。
5. **验证 Hub 用 API 而不是 `docker manifest inspect`**：后者有 CLI 缓存会误报 MISSING；API 有 CDN 延迟，刚 push 完可能看到旧 digest，等十几秒再查。
6. **Actions 里 Node 20 弃用 ANNOTATION 是警告，不是失败**，看到 ✓ Complete job 即成功。
7. **Docker Hub 密码 `songchen521.` 末尾带点**：任何需要登录的场合（如手动 `docker login`）都不能用 `echo | docker login --password-stdin`（CRLF 会混入密码导致 unauthorized）；要用无换行文件重定向。
8. **不要本地 buildx 多架构构建**（Docker Desktop 不稳定，0.3.8 起废弃），全走 GitHub Actions。

## 7. 回滚方案

- 回滚 = 让 Hub `latest` 指回旧版本：把旧提交的 `var version` 恢复 + push（Actions 自动重建），或直接引用旧 digest：`docker pull ssccing/mediamanager@sha256:<旧digest>`。
- NAS 本地若还有旧镜像可直接 `docker tag` + 重启容器，不依赖 Hub。

## 8. 文件清单（改动哪些文件需要重新发布）

| 改了什么 | 需要发布吗 |
|---|---|
| `templates/*.html`（前端网页） | ✅ 必须（打进镜像） |
| `internal/**`、`cmd/**`、`go.mod/go.sum`（后端代码） | ✅ 必须 |
| `static/` | ✅ 必须（镜像内置） |
| `templete.env` | ✅ 必须（镜像内置） |
| `docker-compose.yml`（NAS 上的） | ❌ 不进镜像，改完需用户手动重启容器 |
| `config/user.env`（NAS 上的） | ❌ 运行时挂载，不进镜像 |
