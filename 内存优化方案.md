# Mediamanager 容器内存优化方案

> 记录时间：2026-09-11
> 目标设备：iStoreOS 软路由（172.20.10.2），总内存 2GB
> 观测对象：`Mediamanager` 容器（`ssccing/mediamanager:latest`）
> 当前状态：22.7 MiB / 512 MiB
> **目标：降到 15~17 MiB**
> 方案状态：已定稿，尚未实施

---

## 一、结论摘要

**采用方案 2：任务结束后主动调用 `debug.FreeOSMemory()`。**

核心依据：`inactive_anon = 13.9 MB` 且 `active_anon = 0`，说明当前 13.9 MB 的堆**全是冷页**，是上一轮任务留下的、GC 后已空闲但 Go 的 scavenger 尚未还给 OS 的内存。强制归还即可立即回收，无需改动任何业务逻辑。

预期：`docker stats` 从 **22.7 MiB → 15~17 MiB**。

---

## 二、实测基线（2026-09-11 19:30 前后采集）

### 2.1 容器与主机

| 项目 | 实测值 |
|---|---|
| 主机总内存 | 2 GB（2042648 kB），`MemAvailable` 1139 MB |
| Mediamanager | **22.7 MiB / 512 MiB（4.43%）**，11 PIDs |
| iptv-m3u | 16.97 MiB |
| embyserver | 747.4 MiB（`MemLimit=0`，无限制） |
| `OOMKilled` / `RestartCount` | **false / 0 —— 从未 OOM** |
| STRM 文件数 | 14570 |

### 2.2 进程级 RSS 分解

| 进程 | VmSize | VmRSS | RssAnon | RssFile | Threads |
|---|---|---|---|---|---|
| mmbot (PID 1) | 1.22 G | 24.9 MB | **12.1 MB** | 12.7 MB | 5 |
| MediaWarp (PID 10) | 1.1 G | 8.1 MB | **1.1 MB** | 7.0 MB | 6 |

> `VmSize` 1.22 G 是 Go 的 arena 虚拟地址预留，**不是真实占用**，看 `VmRSS` 即可。

### 2.3 cgroup memory.stat

```
anon            14598144   (13.9 MB)  ← Go 堆 + 栈
file            11894784   (11.3 MB)  ← 二进制映射（clean page）
kernel_stack      196608   (0.19 MB)
sock               20480   (20 KB)
slab             1049864   (1.0 MB)   reclaimable 0.88 + unreclaimable 0.13

inactive_anon   14598144   ← 全部 anon 都是冷的 ★关键
active_anon            0
inactive_file   11894784
```

### 2.4 内存构成与可优化性

`docker stats` 的算法是 `memory.current − inactive_file`，因此：

```
22.7 MiB ≈ anon 13.2 MiB + slab 1.0 + stack 0.19 + 其他（页表/内核）约 8 MiB
```

| 组成部分 | 大小 | 能否优化 |
|---|---|---|
| **anon（Go 堆）** | **13.2 MiB** | ✅ **唯一可动项，需砍到 6~8 MiB** |
| slab（内核 dentry/inode） | 1.0 MB | ❌ 内核自动回收 |
| kernel_stack（11 线程） | 0.19 MB | ❌ 固定开销 |
| 二进制 file 映射 | 19.7 MB | ❌ 已被 `docker stats` 减去，动它无效 |

### 2.5 当前运行配置（`/mnt/mydisk/mediaman/config/user.env`）

```
ENV_CHECK_INTERVAL=180              # 监控每 180 分钟一轮 ★主要内存来源
ENV_TRANSFER_SCAN_INTERVAL=0        # 整理调度器未启用
ENV_STRM_FULL_SYNC=0                # STRM 全量同步未启用
ENV_STRM_CONCURRENCY=3
ENV_STRM_ENABLED=1
ENV_MWARP_ENABLED=1                 # MediaWarp 子进程常驻
```

`docker-compose.yml` 环境变量：`GOMEMLIMIT=256MiB`，**未设 `GOGC`**（默认 100）。

---

## 三、方案原理

Go 的 scavenger（内存拾荒器）是**按需归还**的：它根据 `heapGoal`（由 `GOGC` 推导）和 `GOMEMLIMIT` 决定保留多少内存。当前 `GOMEMLIMIT=256MiB` 遥不可及，scavenger 判断"没有压力"，于是把上一轮任务产生的空闲 span 全部攥在手里 —— 这就是 13.9 MB 冷页的来源。

`debug.FreeOSMemory()` = **强制一次阻塞式 GC + `scavengeAll()`**，绕过 scavenger 的保守判断，立刻把空闲 span 通过 `madvise` 还给内核。

项目在 `internal/web/system.go:326` 已经用过同样手法（查重扫描后归还），属于既有代码风格。

> 已核实：**Go 1.27 未改动 scavenger / `GOMEMLIMIT` / `GOGC` / `FreeOSMemory` 的行为**（见 Go 1.27 Release Notes），本方案前提成立。
> Go 1.27 仅新增了"小对象专用分配函数"（快 30%，二进制 +60KB），与内存归还无关。

---

## 四、实施方案

### 4.1 新建 `internal/memx/memx.go`

（与项目现有 `internal/httpx`、`internal/logx` 命名风格一致）

```go
// Package memx 提供内存归还与统计工具。
//
// 背景：Go 的 scavenger 按需归还内存，在 GOMEMLIMIT 宽松时会长期持有空闲 span，
// 导致 RSS 停在历史峰值水位。任务结束后主动调用 Release 可立即归还给 OS。
package memx

import (
	"log"
	"runtime"
	"runtime/debug"
	"sync"
	"time"
)

const (
	// 空闲量小于此值时，不值得付出一次 GC 的代价
	minWasteBytes = 8 << 20 // 8 MiB
	// 两次归还的最小间隔，防止高频任务反复触发
	minInterval = 2 * time.Minute
)

var (
	mu     sync.Mutex
	lastAt time.Time
)

// Stats 返回堆统计（MiB）。
//
//	allocMiB = HeapAlloc，存活对象占用的堆
//	sysMiB   = HeapSys，向 OS 申请的堆内存（含已空闲未归还的部分）
func Stats() (allocMiB, sysMiB float64) {
	var m runtime.MemStats
	runtime.ReadMemStats(&m)
	return float64(m.HeapAlloc) / (1 << 20), float64(m.HeapSys) / (1 << 20)
}

// Release 强制 GC 并归还空闲内存给 OS。
//
// force=false 时仅在「空闲量 >= 8MiB」且「距上次归还 > 2 分钟」时才执行，
// 适用于事件驱动的、可能较频繁的任务结束点。
// force=true 时无条件执行，适用于确定要长时间空闲的结束点。
//
// 返回是否执行了归还。
func Release(force bool) bool {
	mu.Lock()
	defer mu.Unlock()

	alloc, sys := Stats()
	if !force && (sys-alloc < float64(minWasteBytes)/(1<<20) || time.Since(lastAt) < minInterval) {
		return false
	}

	// 内部已包含一次阻塞式 GC，无需再手动调用 runtime.GC()
	debug.FreeOSMemory()
	lastAt = time.Now()

	_, after := Stats()
	log.Printf("[内存] 已归还空闲内存：HeapSys %.1fMiB → %.1fMiB（存活 %.1fMiB）", sys, after, alloc)
	return true
}
```

### 4.2 插入点 1 ★主战场 —— 监控循环休息前

**文件**：`internal/bot/monitor.go`
**位置**：第 462 行 `log.Printf("[监控] 休息%d分钟...", interval)` 之后

**改动前**：
```go
			log.Printf("[监控] 休息%d分钟...", interval)
			select {
			case <-b.ctx.Done():
```

**改动后**：
```go
			log.Printf("[监控] 休息%d分钟...", interval)
			memx.Release(true) // 刚跑完一轮扫描，即将空闲 180 分钟，强制归还
			select {
			case <-b.ctx.Done():
```

**为什么用 `force=true`**：每 180 分钟才一次，频率极低，且确定要长时间空闲，收益最大化。

**需新增 import**：`"mmbot/internal/memx"`

> 这一步单独做完，预期即可达标（覆盖约 90% 的内存来源）。

---

### 4.3 插入点 2 —— 一轮全量整理完成后

**文件**：`internal/transfer/transfer_scheduler.go`
**位置**：第 133 行

**改动前**：
```go
	log.Printf("全部扫描完成: %v", total)
	return total
```

**改动后**：
```go
	log.Printf("全部扫描完成: %v", total)
	memx.Release(true)
	return total
```

**需新增 import**：`"mmbot/internal/memx"`

> 当前 `ENV_TRANSFER_SCAN_INTERVAL=0` 未启用，但手动触发会走这里。

---

### 4.4 插入点 3 —— 转存联动整理 goroutine 结束前（条件式）

**文件**：`internal/transfer/runtime.go`
**位置**：第 335~338 行的 `if !organizePending` 分支内

**改动前**：
```go
			// 整理期间又有新的转存触发时，补跑一轮
			organizeMu.Lock()
			if !organizePending {
				organizeMu.Unlock()
				break
			}
			organizePending = false
			organizeMu.Unlock()
```

**改动后**：
```go
			// 整理期间又有新的转存触发时，补跑一轮
			organizeMu.Lock()
			if !organizePending {
				organizeMu.Unlock()
				memx.Release(false) // 事件驱动，仅在确实浪费时归还
				break
			}
			organizePending = false
			organizeMu.Unlock()
```

**为什么用 `force=false`**：这是事件驱动的，用户连续转存多个分享时会频繁触发，必须用「8 MiB 阈值 + 2 分钟冷却」保护，避免累积 STW 成本。

**需新增 import**：`"mmbot/internal/memx"`

---

### 4.5 插入点 4 —— STRM 全量同步结束（可选）

**文件**：`internal/strm/runtime.go`
**位置**：第 346~351 行 `safeRun` 的 `defer` 中

**改动前**：
```go
	go func() {
		defer func() {
			r.mu.Lock()
			r.busy = false
			r.mu.Unlock()
		}()
		if action == "full_sync" {
			r.runFullSync()
		}
	}()
```

**改动后**：
```go
	go func() {
		defer func() {
			r.mu.Lock()
			r.busy = false
			r.mu.Unlock()
			memx.Release(false)
		}()
		if action == "full_sync" {
			r.runFullSync()
		}
	}()
```

**需新增 import**：`"mmbot/internal/memx"`

> 当前 `ENV_STRM_FULL_SYNC=0` 未启用，优先级最低。

---

### 4.6 可观测性增强（强烈建议一并做）

当前 `internal/web/system.go:88` 的 `handleSystemUsage` 只返回 cgroup 数字，**看不到 Go 堆**，改完只能靠猜。

**改动前**：
```go
func (s *Server) handleSystemUsage(w http.ResponseWriter, r *http.Request) {
	memMB := readMemMB()
	s.memMu.Lock()
	hist := append([][2]float64(nil), s.memHistory...)
	s.memMu.Unlock()
	if len(hist) > memHistoryMax {
		hist = hist[len(hist)-memHistoryMax:]
	}
	writeJSON(w, http.StatusOK, map[string]any{
		"process": map[string]any{"memory_mb": memMB},
		"history": hist,
	})
}
```

**改动后**：
```go
func (s *Server) handleSystemUsage(w http.ResponseWriter, r *http.Request) {
	memMB := readMemMB()
	allocMiB, sysMiB := memx.Stats()
	var m runtime.MemStats
	runtime.ReadMemStats(&m)
	s.memMu.Lock()
	hist := append([][2]float64(nil), s.memHistory...)
	s.memMu.Unlock()
	if len(hist) > memHistoryMax {
		hist = hist[len(hist)-memHistoryMax:]
	}
	writeJSON(w, http.StatusOK, map[string]any{
		"process": map[string]any{
			"memory_mb":     memMB,
			"heap_alloc_mb": allocMiB, // 存活对象
			"heap_sys_mb":   sysMiB,   // 向 OS 申请的（含空闲未归还）
			"num_gc":        m.NumGC,
		},
		"history": hist,
	})
}
```

**需新增 import**：`"mmbot/internal/memx"`、`"runtime"`

有了这三个字段，就能精确校准 `minWasteBytes` 阈值，而不是拍脑袋定 8 MiB。

---

## 五、预期效果

| 指标 | 现在 | 归还后 |
|---|---|---|
| `HeapSys` | ~10~13 MiB | ≈ `HeapAlloc` + 少量缓冲 |
| `anon` | 13.2 MiB | 约 5~7 MiB |
| **`docker stats`** | **22.7 MiB** | **约 15~17 MiB** ✅ |

日志输出示例（可直接验证收益）：
```
[内存] 已归还空闲内存：HeapSys 12.4MiB → 5.1MiB（存活 3.8MiB）
```

---

## 六、验证步骤

1. 重新构建镜像并部署：
   ```bash
   cd /mnt/mydisk/mediaman
   docker-compose up -d --build
   ```

2. **在任务跑完后 5 分钟**再看（此时 Go 已归还），不要在任务进行中看：
   ```bash
   docker stats --no-stream Mediamanager
   ```

3. 查看归还日志：
   ```bash
   grep '\[内存\]' /mnt/mydisk/mediaman/log/log.log | tail -5
   ```

4. 打开 Web 面板系统页，确认 `heap_alloc_mb` 与 `heap_sys_mb` 的差值已接近 0。

⚠️ **观测盲区提醒**：`internal/web/memory.go:17` 的 `memHistoryMax = 90`，Web 面板内存曲线只有 90 秒窗口，**看不出本方案效果**，请以 `docker stats` 为准。

---

## 七、风险与注意事项

| 风险 | 说明 | 规避方式 |
|---|---|---|
| STW 卡顿 | `FreeOSMemory` 触发完整 GC | 堆仅 13 MB，STW 约 1~5 ms；且只在任务结束时调用 |
| 高频调用拖慢 | 转存联动可能连续触发 | 用 `force=false` + 8 MiB 阈值 + 2 分钟冷却 |
| 重新分配的缺页成本 | 下次任务需重新向 OS 申请 | 监控间隔 180 分钟，影响可忽略 |
| **禁止用于高频路径** | 如 `bot.go` 的 100 ms 轮询、每文件处理 | 本方案只插在任务级结束点 |
| MediaWarp 不受影响 | 独立进程，`FreeOSMemory` 只作用于 mmbot | 其 anon 仅 1.1 MB，无需处理 |

⚠️ **不要重复调用 `runtime.GC()`** —— `debug.FreeOSMemory()` 内部已含一次阻塞式 GC，多调只会加倍 STW 时间。

---

## 八、回滚方法

本方案全部改动的调用点都是**单行插入**，且不触碰任何业务逻辑：

- 直接删除 4 处 `memx.Release(...)` 调用及对应 import 即可完整回滚
- `internal/memx/` 包可保留（无任何副作用）
- 若仅怀疑阈值不合适，把 `minWasteBytes` / `minInterval` 调大即可，**无需删代码**

---

## 九、备选与补充方案（仅在方案 2 未达标时考虑）

### 9.1 降低 `GOGC`（不动代码，改 `docker-compose.yml`）

```yaml
environment:
  - GOGC=40          # 从默认 100 降低
  - GOMEMLIMIT=256MiB  # 保持不变
```

- 预期：`anon` 13.2 → 8~10 MiB，总 18~20 MiB
- 代价：GC 更勤，CPU 从 0.01% 升至约 0.3~1%
- 风险：跑全量同步时 GC 开销上升，任务可能慢 10~20%

> **关于 `GOMEMLIMIT`：不要动。** 它此刻根本不生效（堆 13 MB 远低于 256 MiB），只在重负载时兜底防膨胀，正是需要保留的保险。想靠它压基线需降到 ~32 MiB，会导致全量同步时 GC thrashing，得不偿失。

### 9.2 调小常驻缓存（收益小，优先级最低）

| 缓存 | 位置 | 现上限 |
|---|---|---|
| 302 跳转 LRU | `internal/strm/redirect.go` | 1000 |
| TMDB 搜索缓存 | `internal/tmdb_client.go` | 512 |
| `dirPIDCache` | `internal/transfer/transfer_executor.go` | 4096 |
| `targetListingCache` | 同上 | 500 |

合计仅 0.5~1.5 MiB，且会降低缓存命中率、增加 123 / TMDB 的 API 请求。**不建议动。**

---

## 十、已排查并排除的无效方案（避免重复踩坑）

| 方案 | 排除理由 |
|---|---|
| 缩小 Go 二进制体积 | `docker stats` 会减去 `inactive_file`，二进制变小则 `current` 与 `inactive_file` 同步变小，**差值不变**，对显示数字完全无效 |
| 关闭 MediaWarp | 其 `anon` 仅 1.1 MB，收益极小且丢失 302 跳转 / STRM 直链功能 |
| 优化 `buildPanIndex` 三份索引 | 库规模 14570 个 STRM，实测仅约 5~8 MB，非瓶颈 |
| 降低 `mem_limit`（512m） | 上限不消耗实际内存，是"防失控保险丝"，调低只会增加 OOM 风险 |
| 优化 slab / kernel_stack | 内核开销，不可控 |

---

## 十一、实施清单（勾选式）

- [ ] 新建 `internal/memx/memx.go`
- [ ] `internal/bot/monitor.go:462` 后加 `memx.Release(true)` ★主战场
- [ ] `internal/web/system.go:88` 增加 `heap_alloc_mb` / `heap_sys_mb` / `num_gc`
- [ ] 构建并部署，观察一个监控周期
- [ ] （未达标才做）`internal/transfer/transfer_scheduler.go:133` 加 `memx.Release(true)`
- [ ] （未达标才做）`internal/transfer/runtime.go:337` 加 `memx.Release(false)`
- [ ] （优先级最低）`internal/strm/runtime.go:351` 加 `memx.Release(false)`
- [ ] 根据实测的 `heap_alloc_mb` 校准 `minWasteBytes` 阈值
